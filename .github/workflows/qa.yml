name: QA

on:
  workflow_dispatch:
  merge_group:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    uses: yoriiis/actions/.github/workflows/qa.yml@main
    with:
      node-version: 18
      markdown-status: false
